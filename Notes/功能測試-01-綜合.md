# 功能測試-01-綜合

---

## --測試獨立顯示方程式--

$$S[q(t)] = \int_{t_1}^{t_2} L(q, \dot{q}, t)dt$$

---

##--測試文字中顯示方程式--

這裡有一個$x = 1$，在句子中使用方程式。

---

## --測試希臘符號--

$\alpha\beta\gamma\delta\epsilon\theta\lambda\mu\pi\sigma\phi\omega$

---

## --測試基本方程式--
$\dv[2]{y}{x}$

$\langle\phi\mid\psi\rangle$

力$\vec{F}$的定義

---

## --測試讀文字檔繪圖--

plot_data("Notes/exp02.txt")

---

## --測試畫函數--

plot$$ y = \sin(x), \cos(x)[blue dotted], x/5[green dashed], x∈[-4,8] $$

---

## --測試畫3D函數--

plot3d$$ z = \sin(x)-\cos(y) $$

---

## --測試插圖--
<img src="Notes/pp.png" width="400">

---

## --測試寫程式--

```python
for i in range(2, 10):
    for j in range(2, 10):
        print(f"{i}×{j}={i*j}", end="  ")
    print()
```